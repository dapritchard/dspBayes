#include <string>
#include "InputData.h"

int main(int argc, char* argv[]) {

    // InputData in = InputData(std::string(argv[0]));
    InputData in;
    // std::cout << in.fw_len << "\n";
    // in.print_visual_verif();

    return 0;
}
