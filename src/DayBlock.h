struct DayBlock {

    const int beg_idx;
    const int n_days;

    DayBlock(int beg_idx, int n_days) :
	beg_idx(beg_idx),
	n_days(n_days) (beg_idx, n_days) {
    }
};


// struct PregCyc : public Dayblock {
//     int subj_idx;
// };
